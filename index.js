const app = "I don't do much."

function firstPool(info) {
  var pool_hight = 5

  function pool1(hight) {
    return hight < pool_hight
  }
  
  
  if(pool1(info)){
  	return pool_hight;
  } else {
  	console.log("Sorry, you are not allowed at pool1!")
  } 
  

  return {
  	pool2,
  	pool3
  }
}
